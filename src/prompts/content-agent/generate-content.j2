根据以下信息生成报告章节内容：

章节标题: {{ section_title }}
撰写要求: {{ prompt }}

检索结果:
{% for result in retrieval_results %}
检索结果 {{ loop.index }} (来源: {{ result.source }}):
{{ result.data | tojson(indent=2) }}
{% if not loop.last %}

{% endif %}
{% endfor %}

请生成完整的章节内容，包括：
1. 文本内容（Markdown 格式）
2. 表格数据（如果有）
3. 图表说明（如果有）

返回 JSON 格式：
{
  "text": "章节的文本内容（Markdown格式）",
  "tables": [表格数据数组],
  "images": [图表说明数组]
}

