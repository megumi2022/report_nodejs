# ============================================
# 报告生成系统环境变量配置
# ============================================
# 使用方法：
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 填入你的实际配置值
# 3. 不要将 .env 文件提交到 Git（已在 .gitignore 中）
# ============================================

# ============================================
# LLM 配置（必需）
# ============================================
# 模型名称
MODEL_NAME=qwen3-32b

# API Key（QWEN_API_KEY 或 OPENAI_API_KEY）
QWEN_API_KEY=your_qwen_api_key_here
# 或者使用 OpenAI 兼容的 API Key
# OPENAI_API_KEY=your_openai_api_key_here

# API Base URL
QWEN_API_BASE=https://your-api-endpoint.com/v1
# 或者使用 OpenAI 兼容的 Base URL
# OPENAI_BASE_URL=https://your-api-endpoint.com/v1

# 模型温度（可选，默认 0.7）
TEMPERATURE=0.7

# Agent 最大工具数量（可选，默认 10）
MAX_TOOLS=10

# ============================================
# Embedding 配置（RAG 系统必需）
# ============================================
# Embedding 模型名称
EMBEDDING_MODEL=text-embedding-v4

# DashScope API Key（用于 embedding）
DASHSCOPE_API_KEY=your_dashscope_api_key_here
# 如果使用其他 API，可以使用 QWEN_API_KEY 或 OPENAI_API_KEY

# DashScope Base URL（用于 embedding）
DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# 如果使用其他 API，可以使用 QWEN_API_BASE 或 OPENAI_BASE_URL

# ============================================
# Reranker 配置（可选，用于提升检索精度）
# ============================================
# Reranker API Base URL
RERANKER_BASE_URL=https://dashscope.aliyuncs.com/api/v1/services/rerank
# 注意：Reranker 使用 DASHSCOPE_API_KEY 作为认证

# ============================================
# Supabase 配置（可选，用于向量存储和模板管理）
# ============================================
# Supabase 项目 URL
SUPABASE_URL=https://your-project.supabase.co

# Supabase API Key（Service Role Key 或 Anon Key）
SUPABASE_KEY=your_supabase_key_here

# 向量存储类型：memory（内存，开发/测试）或 supabase（持久化，生产环境）
VECTOR_STORE_TYPE=memory
# 如果使用 Supabase，设置为：
# VECTOR_STORE_TYPE=supabase

# ============================================
# 配置说明
# ============================================
# 1. LLM 配置：至少需要配置 MODEL_NAME、API_KEY 和 API_BASE
# 2. Embedding 配置：RAG 功能必需，至少需要 DASHSCOPE_API_KEY 和 DASHSCOPE_BASE_URL
# 3. Reranker 配置：可选，但建议配置以提升检索精度
# 4. Supabase 配置：可选，如果使用 Supabase 向量存储或模板管理，需要配置
#
# 最小配置（仅运行基本功能）：
#   - MODEL_NAME
#   - QWEN_API_KEY
#   - QWEN_API_BASE
#
# 完整配置（使用所有功能）：
#   - 所有 LLM 配置
#   - 所有 Embedding 配置
#   - Reranker 配置
#   - Supabase 配置（如果使用）
# ============================================
